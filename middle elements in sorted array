#include<stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    scanf("%d",&n);
    int a[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(int i=0;i<n;i++)
    {
        for(int j=i+1;j<n;j++)
        {
            if(a[i]>a[j])
            {
                int b=a[i];
                a[i]=a[j];
                a[j]=b;
            }
        }
    }
    if(n%2==1)
        printf("%d",a[n/2]);
    else
        printf("%d %d",a[n/2-1],a[n/2]);

}#include<stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    scanf("%d",&n);
    int a[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(int i=0;i<n;i++)
    {
        for(int j=i+1;j<n;j++)
        {
            if(a[i]>a[j])
            {
                int b=a[i];
                a[i]=a[j];
                a[j]=b;
            }
        }
    }
    if(n%2==1)
        printf("%d",a[n/2]);
    else
        printf("%d %d",a[n/2-1],a[n/2]);

}
