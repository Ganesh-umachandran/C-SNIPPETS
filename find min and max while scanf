#include<stdio.h>
#include <stdlib.h>
#include<limits.h>

int main()
{
int n;
int min=INT_MAX,max=INT_MIN;
scanf("%d",&n);
for(int i=0;i<n;i++)
{
    int a;
    scanf("%d",&a);
    if (a<min)
        min=a;
    else if(a>max)
        max=a;
}
printf("%d %d",min,max);
}
